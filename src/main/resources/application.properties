server.port=8080

management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always

# Raft node identity (overridden by env vars in Docker)
node.id=${NODE_ID:node1}
cluster.nodes=${CLUSTER_NODES:}
